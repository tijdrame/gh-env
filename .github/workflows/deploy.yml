name: Deployment
on:
  push:
    branches:
      - main
      - dev
env:
  MONGODB_DB_NAME: gha-demo
jobs:
  test:
    env:
      #MONGODB_CLUSTER_ADDRESS: cluster0.15pwqcc.mongodb.net
      MONGODB_CLUSTER_ADDRESS: cluster0.gs9ecre.mongodb.net
      #MONGODB_USERNAME: maximilian
      MONGODB_USERNAME: tijdrame_db_user
      #MONGODB_PASSWORD: X34Mqtpg10D8RGTi
      MONGODB_PASSWORD: eQ2NaTfF66djpvAC
      PORT: 8080
    environment: testing
    runs-on: ubuntu-latest
    steps:
      - name: Get Code
        uses: actions/checkout@v3
      - name: Cache dependencies
        uses: actions/cache@v3
        with:
          path: ~/.npm
          key: npm-deps-${{ hashFiles('**/package-lock.json') }}
      - name: Install dependencies
        run: npm ci
      - name: Run server
        run: npm start & npx wait-on http://127.0.0.1:$PORT
      - name: Run tests
        run: npm test
      - name: Output information
        run: |
          echo "MOGODB_USERNAME is $MONGODB_USERNAME"
          echo "MOGODB_NAME is $MONGODB_NAME"
  deploy:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - name: Output information
        run: |        
          echo "..."
